# Вторая лекция на сегодня, УРА! 🎉

## Инструкция `add` — сложение

Операция сложения выполняется с помощью инструкции:

```nasm
add destination, source    ; destination = destination + source
```

**Пример:**
```nasm
mov rax, 5
mov rbx, 7
add rax, rbx               ; rax = rax + rbx = 12
```

**Ограничения:**
- Оба операнда должны быть одинаковой разрядности (например, оба 64-битные).
- Один из операндов обязательно должен быть регистром или ячейкой памяти, второй — регистром, ячейкой памяти или константой.
- Нельзя складывать две ячейки памяти напрямую.

---

## Инструкция `sub` — вычитание

Операция вычитания выполняется с помощью инструкции:

```nasm
sub destination, source    ; destination = destination - source
```

**Пример:**
```nasm
mov rcx, 30
mov rdx, 12
sub rcx, rdx               ; rcx = 18
```

---

## Инструкции `inc` и `dec` — инкремент и декремент

Эти инструкции увеличивают или уменьшают значение операнда на 1.

```nasm
inc operand    ; operand = operand + 1
dec operand    ; operand = operand - 1
```

**Пример:**
```nasm
mov rsi, 41
inc rsi                    ; rsi = 42

mov rdi, 100
dec rdi                    ; rdi = 99
```