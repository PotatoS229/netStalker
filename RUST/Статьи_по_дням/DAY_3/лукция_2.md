Чесно говроя, я нелюблю долго мяться, я люблю хард кор программирование, по этому мы сегодня погрузимся в мир хардкора, мы спрограммируем с вами веб сервер
Ха-ха-ха, да на хардкор это слабо похоже, многие программисты этим каждым днем занимаются, но в нашем случае если вы ноль в rust и в программировании, то для вас это уж точно будит хард кор.
Давайте начинать
---
Для начала я вам порекомендую сайт с стотьями в котором круто рассказывается, про все нужные и важные нюансы Rust, но надеюсь. что вы меня не забудите, и будите продолжать читать мои статьиэ

https://doc.rust-lang.ru/book/title-page.html

Я только начал писать эту статью, ну боюсь что у меня на нее уйдет куча времени, сейчас 17:48
В конце будит написано сколько сумарно времени было потрачено для того что бы добить эту статью
--- 
Начну я с того что расскажу об основных типах данных
## Типы данных
```rust
let i: i32 = -10; // со знаком
let u: u32 = 10; // без знака
let f: f64 = 3.14; // с плавающей точкой
let b: bool = true; // булевый ти
let c: char = "😀"; // символы Unicode 

//Кортежи
let tup: (i32,f64,u8,&str) = (500,3.14,1,"hello"); // Кортеж из 4 элементов 
let (x,y,z,f) = tup; // Деструктуризация - извлечение всех элементов срузу 
println!("x = {}", x); // 500
println!("y = {}", y); // 3.14
println!("z = {}", z); // 1
println!("f = {}", f) // "hello"
let first = tup.0 // Доступ оп индексу

//Массивы(фиксированная длина)
let arr: [i32; 5] = [1,2,3,4,5];
let first = arr[0];
let same = [3;5] // [3,3,3,3,3]
```
---
## Структуры
```rust
//Создание структуры
struct User {
    username: String,
    email: String,
    active: bool
    id: u32
}
//Создание экземпларя
let user1 = User {
    username: String::from("username"),
    email: String::from("User@gmail.com"),
    active: true,
    id: 12434343,
}
//Изменяемая структура 
let mut user2 = User {};
user2.email = String::from("newmail@gmail.com");
//Сокращенная инициализация структуры 
fn build_user(email: String, username: String) -> User{
    User {
        email,    // email: email
        username, // username: username
        active: true,
        sign_in_count: 1,
    }
}
```
---
## Перечисления 
```rust
//Простое перечисление 
enum IpAddr {
    v4,
    v6
}
//Перечесление с данными
enum Ipaddr{
    v4(u8,u8,u8,u8),
    v6(String)
}
let home = Ipaddr::v4(127,0,0,1)
//Option - встроенное перечисление для обработки null
enum Option<T> {
    Some(T),
    None,
}
let some_number = Some(5);
let absent_number: Option<i32> = None;
```
---
Я думаю все остальное мы разберем уже при разработке простого однопоточного веб-сервера
Давайте сначала обдумаем, что должен делать сервер и что имнно мы будим делать за сервер
1) Мы будим делать однопоточный сервер, который потом мы переделаем в многопоточный
2) Наш срвер будет использовать TCP - то есть горантированная доставка 
3) он будит локальным то есть работать в сети 127.0.0.1 
4) будим использовать протакол http, в дальнейшем изменим на https
Какие задачи он будит выполнять
1) Поднимать сайт
2) Обработка клиент-сервер
3) отслеживание изменений, и в случае изменений бдит выпонять нужное действие 

---
А теперь давайте начианать 
1) инициализация проекта 
```bash
cargo new tcp_server
cd TCP_SERVER 
```
В созданом проете заходи в папку src в main.rs
Данная папка будит для нас основной, в которой будит хранится вся холгика нашего проекта 